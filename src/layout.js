// inportamdo componentes del DOM;

const header = require('./header.js');
const body = require('./body.js');
const footer = require('./footer.js');

const layout =`

    ${header}
    ${body}
    ${footer}
  
 
`;

module.exports = layout;